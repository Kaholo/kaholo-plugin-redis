{
	"name": "kaholo-plugin-redis",
	"version": "1.0.0",
	"description": "Kaholo plugin for integration with Redis.",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Kaholo Group(Amit Alon)",
	"license": "ISC",
	"dependencies": {
		"redis": "^3.1.2"
	}
}